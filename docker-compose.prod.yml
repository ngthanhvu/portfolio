services:
  webmail:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: webmail-prod
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3000
    restart: unless-stopped
